import {StyleSheet} from "react-native";
import {
  colors,
  body2,
  container,
  center,
  bg0,
  h1,
  margin,
  padding,
  row,
  h100
} from "../../config/styles";

const styles = StyleSheet.create({
  header:{
    marginTop: 200
  }
});

export default styles;
